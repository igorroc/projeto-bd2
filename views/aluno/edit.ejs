<!DOCTYPE html>
<html lang="pt-br">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link
			rel="shortcut icon"
			href="/assets/images/logo.png"
			type="image/x-icon"
		/>
		<title>EDIT BD2</title>
		<script src="https://code.iconify.design/iconify-icon/1.0.0/iconify-icon.min.js"></script>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css"
		/>
	</head>

	<body>
		<%- include("../head") %>
		<h1>EDITAR - Aluno (<%= aluno.id %>)</h1>
		<form action="<%= aluno.id %>" method="post">
			<div>
				<label for="">Curso</label>
				<input
					required
					class="form-control"
					type="text"
					name="curso"
					placeholder="<%= aluno.curso %>"
				/>
			</div>
			<div>
				<label for="">Pessoa</label>
				<select required class="form-control" name="matricula">
					<option value="">Selecione uma pessoa</option>
					<% for(const pessoa of dados) { %>
					<option value="<%= pessoa.matricula %>">
						<%= pessoa.nome %>
					</option>
					<% } %>
				</select>
			</div>
			<br />
			<input type="submit" value="Editar" class="btn btn-success" />
			<a href="/select/aluno" class="btn btn-danger">Voltar</a>
		</form>
	</body>
</html>
